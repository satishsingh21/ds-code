// Stack has feature of LIFO

// With array - push/pop is good approach as rebalance not required
// Using liked list - unshift/shift is good approach as we do not need to traverse to last node


// Stack class
class Stack {

	// Array is used to implement stack
	constructor()
	{
		// lets create an empty aary
		this['items'] = [];
		this['size'] = 0;
	}

	// push(item)
	// pop()
}
