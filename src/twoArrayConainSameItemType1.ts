// Problem statement

/* 
    Given there is two array. We need to verify whether one array has all the square element of another array.
    Also there frequency should be same. if its match all condion return true
*/

// Thing to ask 
// Is it pre-fefined which array is smaller 

// Consider array 1 smaller

     // It has time complexity on O(n2)
    // sum(n) {
    //     let sumOfN = 0;
    //     for(let i = 1; i<= n; i++){
    //         sumOfN = sumOfN + i;
    //     }
    //     return sumOfN;
    // }

    // It has time complexity on O(1)
    // sum(n) {
    //     return n*(n+1)/2;
    // }

a = compareArray([1,2,3], [1,4,9]);
b = compareArray([1,2,3], [1,4,9]);
c = compareArray([1,2,3], [1,4,9]);
a = compareArray([1,2,3], [1,4,9]);
a = compareArray([1,2,3], [1,4,9]);